plugins {
    id 'com.android.application'
}

android {
    namespace 'com.exoticsystem.nikkemodtracker'
    compileSdk 35

    defaultConfig {
        applicationId 'com.exoticsystem.nikkemodtracker'
        minSdk 26
        targetSdk 35
        versionCode 30
        versionName '0.3.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
